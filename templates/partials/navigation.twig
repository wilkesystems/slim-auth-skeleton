    <nav class="navbar navbar-default navbar-fixed-top">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="{{ base_url() }}{{ path_for('home') }}"><img src="{{ base_url() }}/images/favicon.png" height="24px" alt="Slim"/></a>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
            <li><a href="{{ base_url() }}{{ path_for('home') }}"><span class="glyphicon glyphicon-home"></span>&nbsp; Home</a></li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
{% if auth.check %}
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="glyphicon glyphicon-user"></span>&nbsp; {{ auth.user.first_name }} {{ auth.user.last_name }} <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><a href="{{ base_url() }}{{ path_for('auth.password.change') }}">Change password</a></li>
                <li><a href="{{ base_url() }}{{ path_for('auth.signout') }}">Sign out</a></li>
              </ul>
            </li>
{% else %}
            <li><a href="{{ base_url() }}{{ path_for('auth.signup') }}"><span class="glyphicon glyphicon-pencil"></span> Sign up</a></li>
            <li><a href="{{ base_url() }}{{ path_for('auth.signin') }}"><span class="glyphicon glyphicon-log-in"></span>&nbsp; Sign in</a></li>
{% endif %}
          </ul>
        </div>
      </div>
    </nav>
